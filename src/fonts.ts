// Fonts are self-hosted in public/fonts and declared via plain @font-face
// in fonts.css (imported once in index.ts) — no network access, no
// JS-driven delayRender needed.
export const condensedFontFamily = 'Oswald';
export const bodyFontFamily = 'Inter';
