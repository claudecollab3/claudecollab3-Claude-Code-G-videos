# frontend/

Web dashboard, built out in **Phase 7** (React + TypeScript + Vite,
WebSocket-driven live updates for balance/equity/trades/AI confidence/news).

A minimal placeholder app lives here now so `docker/Dockerfile.frontend` and
`docker-compose.yml` have something to build; the full dashboard replaces it
in Phase 7.
