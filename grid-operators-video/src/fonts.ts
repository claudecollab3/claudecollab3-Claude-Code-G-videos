// Self-hosted (base64-inlined in fonts.css, imported once in index.ts) —
// no network access needed at render time, and no risk of the delayRender
// hang that repeated network font loads caused in an earlier project.
export const captionFontFamily = 'Arvo';
